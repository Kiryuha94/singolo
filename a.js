function multiTable(number) {
    const array = Array.from({length: 10})
    return array.map((_,index) => `${index + 1} * ${number} = ${(index + 1) * number}`)
   }
   console.log(multiTable(3))


   const get5 = () => {
       const a = 5
       return a
   }

   return 5

   const get5 = () => 5
   